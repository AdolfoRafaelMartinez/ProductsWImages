<%= form_for(@t0) do |f| %>
  <% if @t0.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@t0.errors.count, "error") %> prohibited this t0 from being saved:</h2>

      <ul>
      <% @t0.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :path %><br />
    <%= f.text_field :path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
